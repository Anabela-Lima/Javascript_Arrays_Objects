// create an array 
shoppingList = ["milk", "water", "beer", "coffee"]
//use for loop
for(item in shoppingList){console.log(item);}
// we used a for loop to print the index of items
for(item of shoppingList){console.log(item.toUpperCase);}
for(item of shoppingList) {console.log(item)}
// used of to print actual items in shoppingList
// lets move on 
for(i=0; i<shoppingList.length; i++){console.log(shoppingList[i]);

shoppingList
}
// starting for loop again 
shoppingList = ["milk", "coffee", "water"]
for(i = 0; i< shoppingList.length; i++){console.log(shoppingList[i]);
}